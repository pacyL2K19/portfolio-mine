import styled from 'styled-components';
// import { theme } from '../../core/theme';

export const Menu = styled.div<Nav>`
    background-color: ${props => props.theme.colors.navBgColor};
    height: 80px;
    width: 100%;
`