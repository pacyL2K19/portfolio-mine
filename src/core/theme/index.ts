import {colors} from './colors';
import { primaryFonts, secondaryFonts, titleFonts } from './fonts';

export const theme: Theme = {
    fonts: {
        title: titleFonts,
        secondary: secondaryFonts,
        primary: primaryFonts
    },
    colors
}